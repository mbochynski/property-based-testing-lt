import { SimpleComponent } from './SimpleComponent';

describe('SimpleComponent', () => {
    let wrapper;
    beforeEach(() => {
        wrapper = shallow(<SimpleComponent/>);
    });

    it('should ')
});